# Core application
pandas
altair
# Intel macOS is limited to PyTorch 2.2, whose wheels use the NumPy 1.x ABI.
numpy<2
streamlit
python-dotenv
supabase

# Audio
# OpenAI Whisper uses local PyTorch inference and requires the ffmpeg system command.
openai-whisper
# Keep Whisper's JIT dependencies on versions that publish Python 3.10 wheels
# for Intel macOS. Newer llvmlite releases otherwise require a local LLVM build.
numba==0.62.1
llvmlite==0.45.1
torch==2.2.2
torchvision==0.17.2

# PDF and OCR
PyMuPDF
pdfplumber
pytesseract
# Optional NER for PDF field extraction; the processor falls back to regex
# when spaCy or its model is unavailable.
spacy
# 49.x no longer publishes a wheel compatible with Intel macOS.
cryptography==48.0.1

# Image and video
# Headless OpenCV avoids desktop GUI libraries that are unavailable on Cloud Run.
opencv-python-headless==4.10.0.84
ultralytics==8.4.70
Pillow

# Testing
pytest
