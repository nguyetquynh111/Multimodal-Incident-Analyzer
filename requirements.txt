# Multimodal Incident Analyzer — all dependencies (app + every modality).
#
# Versions are intentionally left open so pip selects wheels compatible with
# your Python. Tested on Python 3.13 (Apple Silicon); also resolves on 3.10.
# The audio module additionally needs the FFmpeg system command for real
# Whisper transcription:  macOS -> brew install ffmpeg

# Core: integration, Streamlit dashboard/app, Supabase
pandas
streamlit
python-dotenv
supabase

# Audio — Whisper speech-to-text (pulls torch + numba automatically)
openai-whisper
torch

# PDF and OCR
PyMuPDF
pdfplumber
pytesseract

# Image and video
opencv-python
moviepy
imageio
Pillow

# Testing
pytest
